## v13 UI 개선
- 점검 분류 아이콘 카드 적용
- 대시보드 핵심 현황 3개 한 줄 배치
- 빠른 메뉴 4개 한 줄 배치

# HOMES FM

모바일 룸체크 및 보수관리 웹앱입니다.

## 이번 수정사항
- CS 요청 선택 시 요청자 입력란 표시 및 필수 입력
- 양호 항목에도 상태 설명/확인 메모 입력 가능
- 미점검 항목이 있어도 저장 가능
- 목록에 미점검 개수 표시
- 리포트에 미점검 항목 목록 및 개수 표시

데이터는 현재 브라우저의 localStorage에 저장됩니다.

## v5 추가 기능
- 수리 단가 관리(데모 관리자 PIN: 2468)
- 보통/불량 선택 시 기본 예상단가 자동 적용
- 실제비용, 제품코드, 구입처, 업체, 증빙번호 기록
- 보수 전/후 사진 여러 장 및 수리이력 페이지
- 리포트 예상/실제 비용 합계
- `supabase_schema.sql`: 운영용 Auth/RLS 기반 DB 구조

> 현재 HTML 데모는 localStorage를 사용합니다. 실제 다중 사용자 운영은 Supabase 프로젝트 URL/키 연결과 Storage 버킷 설정이 추가로 필요합니다.


## v7 변경사항
- 모든 보수 대상 완료 시 미점검 항목이 남아 있어도 정비 완료 상태로 전환
- 예상 비용은 단가표 기준 읽기 전용
- 실제 비용 입력 및 비용 부담 주체 선택(투자사/홈즈/세입자/기타)
- 리포트 확대 범위 80~220%, 실제 화면 확대 적용

## v9 변경사항
- 점검 분류 4개 한 줄 고정
- 지점→층→호실 즉시 이어지는 인라인 선택 UI
- 체크 카테고리 기본 닫힘
- 기타 사용자 항목 여러 개 추가/삭제
- 점검 단계에서 비용 부담 주체 지정
- 보수 화면 순서 재구성 및 전·후 사진 설명 추가
- 상태별 보수 목록 필터와 로그아웃 버튼
- 룸체크/보수/완료 상태별 리포트 제목
- 비용 입력 천 단위 쉼표 표시


## v16 변경사항
- 더보기 페이지 아이콘 통일
- 홈 최근 작업 지점/분류/점검자/일자 필터 및 호실 검색 추가


## v17 개선
- 누락 아이콘 자동 복구 및 전체 화면 아이콘 일관성 강화
- 보수 처리 완료 체크박스 디자인 개선
- 홈 최근 작업 필터에 지점·분류·점검자·점검일·호실 라벨과 아이콘 적용
- 입력·카드·버튼 시각 품질 및 터치 피드백 개선

## v19 계정 기능
- 더보기 > 로그인/계정에서 사용자 등록 및 로그인
- 일반 등록 이메일은 `@homes.global`로 제한
- 초기 관리자 예외 계정: `alldayworking@naver.com` / 초기 비밀번호 `0338`
- 이메일과 이름 필수, 부서와 전화번호 선택 입력
- 관리자만 수리 단가 등록·수정·삭제 및 사용자 권한 변경 가능
- 현재 HTML 데모는 localStorage 기반입니다. 운영 환경에서는 `supabase_schema.sql`과 Supabase Auth/Edge Function을 연결하세요.

## v20 계정 관리
- 초기 시스템 관리자: `alldayworking@naver.com`, `alldayworking24@gmail.com`
- 초기 관리자 비밀번호: 별도 전달된 초기 비밀번호
- 일반 사용자는 관리자가 계정 페이지에서 등록합니다.
- 일반/운영 계정 이메일은 `@homes.global` 도메인만 허용합니다.
- 신규 사용자의 임시 비밀번호는 `0338`이며 첫 로그인 시 비밀번호 변경이 강제됩니다.
- 이메일과 이름은 필수이며 부서, 전화번호, 권한을 함께 등록할 수 있습니다.

> 현재 데모 버전의 계정 정보는 브라우저 localStorage에 저장됩니다. 실제 운영 환경에서는 Supabase Auth와 서버 측 권한 정책으로 이전해야 합니다.

## v22 단가 우선순위
- HOMES 조정단가가 등록되어 있으면 해당 금액을 우선 적용합니다.
- HOMES 조정단가가 없으면 LH 기준단가를 적용합니다.
- 단가관리 화면에서 LH 기준금액과 HOMES 조정금액을 함께 비교할 수 있습니다.
- HOMES 조정단가를 삭제하면 자동으로 LH 기준단가로 복귀합니다.
- 포함된 LH 초기값은 구조 확인용 샘플이므로 실제 운영 전 공식 계약·고시 자료로 검증·교체해야 합니다.


## v23 변경사항
- 리포트 요약 카드 클릭 시 해당 상세 항목으로 스크롤 이동
- 시스템 관리자 계정 UI 중앙 정렬 개선
- 각 수리 항목 카드에서 세부 작업을 바로 추가 가능


## v26 변경사항
- 점검 분류 순서 변경: 퇴실 → 입실 전 → CS 요청 → 정기
- 점검자 자동 입력: 로그인한 사용자 이름(없으면 이메일)이 자동으로 채워지며 수정 불가
- 리포트 확대·축소 바: 스크롤을 내리면 자동으로 숨겨지고, 위로 올리면 다시 표시
- 파비콘(브라우저 탭 아이콘)에 HOMES BI 로고 적용
- 비용 부담 주체를 룸체크 신규·수정 화면에서 입력 가능(보통/불량 항목), 입력값이 보수·리포트에 그대로 반영(룸체크 입력값 우선)
- 단가 출처 배지 색상: LH 기준=녹색, HOMES 조정=HOMES 골드(#FFCF0D)
- 보수 목록의 "보수 상세"·"리포트" 버튼 크기 균형 조정(리포트 버튼에 라벨 추가)


## v27 변경사항
- 하단 홈 아이콘을 HOMES 하우스 마크(스마일) 형태로 변경
- 신규(＋) 버튼 배경을 HOMES 골드(#FFCF0D)로 변경
- 하단 탭 선택 시 배경을 HOMES 골드 계열로 변경
- 룸체크 등록자/수정자 이력 저장 및 리포트 표시 — 최초 등록자와 최근 수정자, 각 일자 표시
- 보수 상세 화면 스크롤 시 상단에 "○○ ○○○호 · 보수" sticky 바 고정 표시
- 계정(시스템 관리자) 프로필 아바타의 이니셜 배지를 HOMES 로고로 교체
- 시스템 관리자 계정 페이지에 "룸체크·수리 이력 관리" 추가 — 호실별 이력 삭제 가능
  - 삭제는 논리 삭제(soft delete) 방식: `deleted` 플래그로 목록·리포트·보수에서 숨김 처리하고 데이터는 보존, 복구 가능
  - Supabase 스키마에 `room_checks` 테이블(등록자/수정자/deleted_at)과 `repair_history.deleted_at`, 활성 뷰(`*_active`) 추가


## v28 변경사항
- 로그인 관문(전체 강제) 추가: 앱 최초 접근 시 로그인 화면이 먼저 표시되며, 로그인해야 홈·룸체크·보수관리·리포트 등 모든 화면을 사용할 수 있음
  - 미로그인 상태에서는 하단 내비게이션·햄버거 메뉴가 숨겨지고, 다른 화면으로 이동해도 로그인 화면으로 되돌아옴
  - 로그아웃 시 다시 로그인 화면으로 잠김
  - 초기 시스템 관리자 계정으로 최초 로그인 가능(최초 로그인 시 비밀번호 변경 화면으로 자동 이동)


## v29 변경사항
- 홈 대시보드에서 '실제 보수비' 요약 박스 제거(전체 룸체크·보수중·보수완료 3개 카드만 표시)
- 하단 '더보기' 버튼은 일반 사용자(viewer)에게 표시하지 않음 — 해당 자리에는 '계정' 버튼이 표시되어 로그아웃/내 정보로 이동 가능(운영·시스템 관리자는 기존처럼 '더보기' 표시)
- 계정 페이지를 로그아웃 페이지로 명확화: 모든 사용자가 하단 계정(또는 더보기→계정)에서 로그아웃 가능, 닫기 시 홈으로 이동


## v30 변경사항
- 로그인 화면 및 첫 로그인(비밀번호 설정) 화면에서 Enter 키로 버튼 클릭과 동일하게 제출되도록 처리
- 첫 로그인 화면의 '현재 임시 비밀번호'를 자동 표시 — 로그인 시 입력한 비밀번호를 그대로 채우고, 없으면 기본 임시 비밀번호(0338)로 프리필


## v31 변경사항
- 지점·층·호실 구조를 하드코딩(DATA 공식)에서 **저장형 데이터로 전환**(DB화) — 룸체크의 지점/층/호실 선택이 저장된 설정에서 생성됨
- 관리자용 **'지점·호실 관리'** 화면 추가(더보기 → 지점·호실 관리, 관리자 전용)
  - 지점 추가/삭제/이름 변경, 층 추가/삭제, 층별 호실 수 설정
  - 호실은 숫자(개수 자동 생성, 예 8→301~308) 또는 쉼표 직접 나열(예 301,302,305) 지원 — 불규칙 호실 대응
  - 지점별 총 세대수·층수 실시간 표시, 저장 시 룸체크 화면에 즉시 반영, 되돌리기 지원
  - 최초 실행 시 기존 값으로 자동 시드(남영 12층 등 기존 호실 배치 그대로 보존) 후 편집 가능
- Supabase 스키마에 지점 마스터 테이블(`branches`: name/floors(jsonb)/is_active)과 정규화용 `branch_units` 테이블 추가

## Supabase 지점·동·층·호실 연동
신규 룸체크 화면은 `branches`와 `units` 데이터를 Netlify Function으로 읽습니다.
Netlify 사이트 설정 → Environment variables에 다음 값을 등록하세요.

- `SUPABASE_URL` = `https://hunoualsfmfckalwbilt.supabase.co`
- `SUPABASE_ANON_KEY` = Supabase 프로젝트의 Publishable/anon key

키가 아직 설정되지 않은 경우에도 현재 DB 기준 오프라인 캐시가 표시됩니다. 키 등록 후에는 DB 변경사항이 새로고침할 때 자동 반영됩니다.
아코모가산바이홈즈는 DB의 `-101동`, `-201동` 지점을 화면에서 하나의 지점으로 묶어 `지점 → 동 → 층 → 호실` 순서로 표시합니다.

## v33 변경사항
- 신규 등록 첫 화면을 로그인 페이지로 고정
- 지점 표시 순서: 선정릉 → 남영 → 안암 → 아코모가산바이홈즈 → 아코모회기바이홈즈
- 아코모가산 동 선택을 101동/201동 라디오 카드 방식으로 개선
- 다른 지점 선택 시 기존 동/층/호실 선택값 자동 초기화
- 룸체크 목록 삭제 버튼은 시스템 관리자에게만 표시되며 삭제 함수에도 권한 검증 적용

## v35 공유 사진·기록 설정
사진은 브라우저 localStorage가 아니라 Supabase Storage의 `homes-fm-photos` 버킷에 저장되고, 룸체크 기록은 `homes_fm_records`에 동기화됩니다. 따라서 다른 휴대폰이나 PC에서도 같은 기록과 사진을 볼 수 있습니다.

Netlify 환경변수에 아래 값을 반드시 등록하세요.
- `SUPABASE_URL`: `https://hunoualsfmfckalwbilt.supabase.co`
- `SUPABASE_ANON_KEY`: 기존 지점/호실 조회용 anon 또는 publishable key
- `SUPABASE_SERVICE_ROLE_KEY`: Supabase Dashboard → Project Settings → API → service_role key

`SUPABASE_SERVICE_ROLE_KEY`는 프런트 코드에 넣지 말고 Netlify 환경변수에만 저장해야 합니다.
