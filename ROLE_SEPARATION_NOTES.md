# HOMES FM 역할 분리 정리

## ⚠ 2026-09-11 긴급 수정 — 계정 화면 오류 (이전에 전달드린 파일에 포함된 버그)

관리 화면을 걷어내는 과정에서 `renderAccount()` 함수가 삭제된 `#userAdminPanel`/`#userList`
요소를 안전장치 없이 직접 참조하는 코드가 남아 있었습니다. 이 상태로는 **로그인한 모든
사용자가 계정 화면(로그인 직후 자동 이동 포함)에 들어갈 때마다 JavaScript 오류가 발생**해서
화면 일부가 제대로 갱신되지 않았을 것입니다. 이번 파일에서 해당 코드를 완전히 제거해
수정했습니다. **이전에 전달받은 파일로 이미 배포하셨다면 반드시 이 버전으로 다시
교체해 주세요.**

## 수정 목표
- homes-fm: 실무자용. 현장 점검, 룸체크, 보수 사진, 보수 처리 상태 확인에 집중.
- homes-fm-admin: 관리자용. 지점별 현황, 지연, 비용, 데이터 품질, 사용자/권한, 업체/단가 기준 관리에 집중.

## 이번 수정 내용
### homes-fm-main
- 더보기 화면을 `실무자 메뉴`로 정리했습니다.
- 일반 실무자에게 불필요한 수리 단가, 지점·호실 DB, 체크리스트 DB, 업체 관리, 데이터 백업 메뉴를 기본 숨김 처리했습니다.
- 운영 관리자/시스템 관리자 계정만 관리 메뉴가 보이도록 `adminMode` 클래스를 적용했습니다.
- 일반 계정이 관리 페이지 URL로 직접 접근하려고 하면 안내 후 실무자 메뉴로 돌려보내도록 방어 코드를 추가했습니다.
- 기존 데이터 구조, Supabase 테이블, localStorage 키는 변경하지 않았습니다.

### homes-fm-admin-main
- 사이드 메뉴명을 관리자 관점으로 재정리했습니다.
- 상단 설명을 `실무자 입력 데이터 기반의 종합 관리 화면`으로 바꿨습니다.
- 대시보드 최상단에 관리자 화면 역할 안내 박스를 추가했습니다.
- 관리자에서 보수 요청을 새로 넣는 버튼은 `관리자 수기 등록`으로 명확히 바꿨습니다.
- 기존 데이터 조회/저장 로직은 유지했습니다.

## 배포 전 확인
1. 일반 사용자로 homes-fm 로그인 후 더보기 메뉴에 관리 기능이 숨겨지는지 확인.
2. manager/admin 계정으로 homes-fm 로그인 후 관리 메뉴가 필요한 경우에만 보이는지 확인.
3. homes-fm-admin 로그인 후 기존 데이터가 그대로 보이는지 확인.
4. homes-fm에서 등록한 룸체크/보수 데이터가 homes-fm-admin 대시보드에 반영되는지 확인.

## 2026-09-08 추가 수정 — 실무자 필터 통일 / 룸체크-보수관리 연결

### 1. 화면별 필터 기준 통일
실무자가 많이 사용하는 홈 최근 작업 필터 기준을 룸체크 목록, 보수관리, 수리 이력에도 맞췄습니다.

공통 적용 필터:
- 지점
- 점검 분류
- 점검자
- 조회 월
- 시작일~종료일 기간
- 호실 검색

화면별 추가 필터:
- 룸체크 목록: 보수 상태
- 보수관리: 상단 탭 전체/보수 예정/보수중/보수완료 유지
- 수리 이력: 비용 부담 주체 유지

### 2. 룸체크에서 바로 보수관리 이동
홈 최근 작업, 룸체크 목록, 룸체크 상세 화면에서 바로 보수관리 화면으로 들어갈 수 있게 했습니다.

- 홈 최근 작업 카드: 리포트 / 보수관리 버튼 추가
- 룸체크 목록: 보수관리 아이콘 버튼 추가
- 룸체크 상세 하단 메뉴: 보수관리 버튼 추가

보수 대상이 없는 룸체크 기록에서 보수관리를 누르면 안내 문구가 표시됩니다.

### 3. 데이터 유지
Supabase 테이블 구조와 기존 데이터 저장 방식은 변경하지 않았습니다.
UI 필터와 화면 이동 기능만 보강했습니다.

## 2026-09-11 진짜 역할 분리 (v2) — 관리자 기능을 homes-fm-admin으로 완전히 이관

09-08까지의 수정은 관리 메뉴를 "숨기기만" 한 것이었고, 실제 코드는 여전히 homes-fm 안에 그대로
남아 있어 운영/시스템 관리자는 homes-fm에서도 관리 기능을 계속 쓸 수 있었습니다. 이번 수정은
그 기능 자체를 homes-fm에서 들어내 homes-fm-admin으로 옮겼습니다.

### homes-fm에서 완전히 제거한 화면
- 수리 단가 관리(등록/수정) 및 신규 단가 등록 화면
- 지점·동·층·호실 DB 관리 화면
- 체크리스트 DB 관리 화면
- 보수 업체 관리 화면(등록/수정/삭제)
- 계정 화면의 "사용자·권한 관리" 카드(신규 등록/권한변경/비번초기화/삭제)
- 계정 화면의 "룸체크·수리 이력 관리" 카드(논리삭제/복구/영구삭제)
- 위 화면들로 가는 사이드바·더보기·홈 빠른메뉴의 모든 버튼

homes-fm에는 실무자가 실제로 쓰는 룸체크 입력/목록, 보수관리, 리포트·PDF, 수리 이력 조회,
캘린더, 계정(로그인/내 정보/비밀번호 변경)만 남았습니다. 지점·호실 선택, 체크리스트 적용,
업체 선택 드롭다운, 단가 참고용 데이터를 불러오는 내부 함수는 그대로 남겨 실무자 화면이
정상 동작하도록 했습니다.

### 안전을 위해 함께 고친 부분
- `go(id)`가 이제 없는 화면 id로 호출되면(예: 즐겨찾기·이전 화면 상태로 `#branchAdmin` 등에
  진입 시도) 오류 없이 "더보기"로 안내하도록 방어 코드를 고쳤습니다. (이전에는 관리자 계정이
  옛 링크로 들어오면 빈 화면에서 스크립트 오류가 날 수 있었습니다.)
- `netlify/functions/admin-user.js`의 "영구삭제"(`delete_record`) 기능이 존재하지 않는
  `inspection_records` 테이블을 가리키고 있던 버그를 고쳐 실제 테이블(`homes_fm_records`)을
  삭제하도록 했고, 사진 스토리지 정리 기능을 추가했습니다. 이 함수는 homes-fm-admin의
  "이력 관리" 화면에서도 그대로 호출합니다.

### 데이터 유지
Supabase 테이블 구조·기존 데이터는 변경하지 않았습니다. `branches`/`branch_units`/
`checklist_templates`/`maintenance_vendors`/`homes_fm_records`는 기존과 동일한 컬럼·같은
upsert 방식(homes-fm이 쓰던 것과 동일)으로만 접근합니다.

### 배포 전 꼭 확인해 주세요
1. homes-fm: 일반 사용자·운영 관리자·시스템 관리자 계정 모두 더보기/사이드바에 관리 메뉴가
   전혀 보이지 않는지 확인.
2. homes-fm: 룸체크 입력 시 체크리스트가 정상 표시되는지, 보수관리에서 업체 선택과 단가
   참고 표시가 정상 동작하는지 확인(관리 화면 제거가 이 기능들을 건드리지 않았는지 확인).
3. homes-fm-admin: "지점·호실 관리"에서 지점 추가·호실 목록 저장 후 homes-fm 룸체크 화면의
   지점/호실 선택 목록에 즉시 반영되는지 확인.
4. homes-fm-admin: "체크리스트 양식 관리"는 이번 버전에서 JSON 직접 편집 방식으로 단순화
   되었습니다. 기존 양식을 열어 JSON 형태를 확인한 뒤 따라서 새 양식을 만들어 주세요.
5. homes-fm-admin: "업체·단가 기준" 하단의 "수리 단가표"는 Supabase `user_profiles` 테이블에
   admin 권한으로 등록된 계정만 저장할 수 있습니다. 실제 로그인 권한 판단은 `app_users`
   테이블 기준이라, `user_profiles`에 같은 계정이 없으면 저장 시 권한 오류가 날 수 있습니다.
   오류가 나면 Supabase에서 `user_profiles` 테이블에 해당 관리자 계정을 등록해 주세요.
6. homes-fm-admin: "이력 관리"의 영구삭제는 되돌릴 수 없습니다. 먼저 삭제(숨김)/복구로
   충분히 테스트한 뒤 영구삭제를 사용해 주세요.

## 2026-09-11 홈즈 공식 웹·UI 디자인 가이드 색상 정렬

homes-fm-admin은 이미 홈즈 공식 웹·UI 가이드 토큰(골드 `#F2C94C`, 블랙 `#1A1A1A`, 회색 스케일,
DM Sans·Pretendard)을 그대로 쓰고 있어 별도 수정이 필요 없었습니다.

homes-fm(실무자 앱)은 이전에 "인쇄용 골드"(`#FFCF0D`)와 따뜻한 크림색 배경(`#F4F2EC`)을 쓰고
있었습니다. 인쇄물용 골드는 화면(웹·UI)용이 아니라서, 다음 값만 웹·UI 가이드 기준으로
바꿨습니다(레이아웃·구조·클래스는 전혀 건드리지 않고 색상 값만 교체했습니다).

- 포인트 골드(버튼/배지/번호 타일/카메라 버튼 등): `#FFCF0D` → `#F2C94C`
- 골드 강조/호버(`--gold2`, `--goldink`): `#FFC107`/`#D99E00` → `#D4A843`(가이드 gold-dark)
- 카드·구분선 색(`--line`): 따뜻한 탠 톤 `#E7DDC6` → 중립 회색 `#EEEEEE`
- 배경(`--cream`): 크림색 `#F4F2EC` → 중립 오프화이트 `#FAFAFA`
- 상태색(양호/주의/불량): 가이드의 green/orange/red 값으로 통일

폰트(DM Sans + Pretendard)는 이미 적용되어 있어 그대로 뒀습니다. 실제 화면에서 색이 바뀌어
보이는지 배포 후 한 번 확인해 주세요.

## 2026-09-11 자잘한 UI 버그 수정 + "업체 의뢰함" 상태 추가

### 룸체크 목록 카드 버튼 한 줄 정렬
룸체크 목록 카드의 하단 버튼(점검상세·점검수정·보수관리·리포트·삭제) 5개가 그리드 4칸에
꽉 채워져 있어 1개가 다음 줄로 밀려났습니다. 그리드를 5칸으로 늘려 한 줄에 다 나오게
고쳤습니다.

### 홈즈FM 도배 현황(admin) 지점 클릭 버그
homes-fm-admin "도배 현황" 화면에서 지점 카드를 클릭하면 상단 지점 필터가 그 지점으로
바뀌면서 다른 지점 카드가 전부 사라져 되돌아갈 방법이 없었습니다. 이제 지점 카드 목록은
필터와 무관하게 항상 전체 지점을 보여주고, 선택된 지점은 카드가 진하게 표시되며, 같은
지점을 다시 클릭하면 선택이 풀립니다. "전체 지점 다시 보기" 버튼도 추가했습니다.

### "업체 의뢰함" 상태 신규 추가
지금까지는 업체명을 입력만 해도 '배정'으로 표시되어, 실제로 업체에 연락해서 의뢰했는지
그냥 담당자를 메모만 해둔 건지 구분이 안 됐습니다. 이번에 상태 단계 자체를 늘렸습니다.

**새 흐름**: 접수 → 배정(업체명만 지정) → **의뢰됨(실제로 연락함)** → 진행중 → 완료

- homes-fm 보수 상세 화면의 업체 선택 아래에 "실제로 업체에 연락해서 의뢰함" 체크박스를
  추가했습니다. "보수 진행 중"이나 "보수 처리 완료"를 체크하면 이 항목도 자동으로 체크됩니다
  (이미 연락했다는 뜻이므로).
- homes-fm 보수관리 목록 상단 탭에 "업체 의뢰함" 탭을 추가했고(전체/보수예정/업체
  의뢰함/보수중/보수완료), 목록 카드·리포트·엑셀 내보내기에도 "업체 의뢰함" 표시가
  반영됩니다.
- homes-fm-admin의 보수 요청 파이프라인(보드), 상태 탭, 요청 배지에도 "의뢰됨" 단계가
  똑같이 추가되어 관리자가 "아직 업체 연락도 안 한 건"과 "연락은 했지만 아직 시작 전인
  건"을 구분해서 볼 수 있습니다.

기존 저장된 데이터는 새 필드(`vendorRequested`)가 없으므로 전부 "아직 의뢰 전" 상태로
보입니다. 실제로 이미 업체에 연락해 진행 중이던 건은 보수 상세를 한 번 열어서 체크박스를
확인·저장해 주시면 정확한 상태로 반영됩니다.

## 2026-09-11 "보수 미완료 처리" 상태 추가

현장에서 보수를 더 진행하지 못하고 종료시키는 경우(세입자 퇴실, 부품 단종, 연락 두절 등)를
"완료"와 구분하기 위해 체크박스를 하나 더 추가했습니다.

- homes-fm 보수 상세 화면에 "보수 진행 못하고 종료(미완료 처리)" 체크박스를 추가했습니다.
  "보수 진행 중"/"보수 처리 완료"와 서로 배타적입니다(하나를 체크하면 나머지는 자동 해제).
  체크하면 "미완료 처리 사유" 메모도 남길 수 있습니다.
- 이 상태로 닫힌 항목은 목록·탭에서는 "보수완료" 쪽으로 묶여 더 이상 미완료로 계속 남지
  않지만, 항목 배지·리포트·엑셀에는 "미완료 처리"로 정확히 구분되어 표시됩니다.
- homes-fm-admin의 요청 파이프라인·상태 탭·배지에도 "미완료처리" 상태가 반영되어, 관리자가
  "정상 완료"와 "못 끝내고 닫은 건"을 구분해서 확인할 수 있습니다. 대시보드의 "완료율"·
  "완료 비용"은 정상 완료 건만 집계하도록 했습니다.

## 2026-09-11 체크리스트 양식 편집기를 JSON 입력 → 시각적 편집기로 교체

homes-fm-admin의 "체크리스트 양식 관리"가 JSON을 직접 타이핑하는 방식이라 실수하기
쉬웠습니다. homes-fm에 이미 있던(관리 화면 이관 전) 분류·항목 단위 시각적 편집기를 그대로
옮겨왔습니다.

- 분류(예: "주방") 추가/삭제/순서 변경, 분류 안에 체크 항목(이름·확인 내용·입력 유형)
  추가/삭제를 화면에서 직접 클릭·입력으로 처리합니다. JSON을 몰라도 됩니다.
  입력 유형은 homes-fm과 동일한 7종(일반 상태/검침값/가스 검침/누수 여부/보일러 정보/
  공유기 정보/수기 입력) 중에서 고릅니다.
  "미리보기" 버튼으로 실제 점검 화면에 어떻게 보일지 먼저 확인할 수 있습니다.
- **중요한 버그 수정**: 이전 JSON 입력 방식에서 "새 양식" 생성 시 `definition`을
  `{categories:[]}` 형태로 저장하고 있었는데, homes-fm이 실제로 읽는 구조는 배열
  (`[{name, items:[...]}]`)입니다. 이 불일치 때문에 admin에서 새로 만든 양식은 homes-fm
  에서 정상적으로 표시되지 않았을 것입니다. 이번 교체로 완전히 동일한 배열 구조를 쓰도록
  고쳤습니다. **혹시 이전 버전의 admin에서 이미 "새 양식"을 만들어 저장하신 적이 있다면,
  그 양식은 다시 열어서 분류·항목을 확인/재저장해 주세요.**

## 2026-09-11 FM 화면 전수 점검 (계정 화면 버그 발견 계기)

계정 화면 버그를 계기로, 관리 화면 제거 작업이 다른 곳에 비슷한 문제를 남기지 않았는지
homes-fm 전체를 점검했습니다.

- **점검 상세 하단 메뉴 버튼 줄바꿈**: 목록/보수관리/리포트/위로 4개 버튼이 3칸 그리드에
  끼여 있어 1개가 다음 줄로 밀려나던 것을 4칸으로 수정했습니다.
- 삭제된 관리 화면(단가/지점호실/체크리스트/업체/사용자관리)과 연결된 나머지 코드는 전부
  "호출할 방법이 없는" 상태임을 하나씩 확인했습니다. 실제로 문제였던 건 계정 화면 건
  하나뿐이었습니다.
- **"업체 의뢰함" 상태 반영 보완**: 홈 화면 요약 타일("보수 예정")과 룸체크 목록의 "전체
  상태" 필터가 새로 추가된 "업체 의뢰함" 상태를 놓치고 있던 것을 고쳤습니다.
  - 홈 화면 요약: "보수 예정" 타일과 그 필터가 "업체 의뢰함" 건도 함께 집계·표시하도록
    통일했습니다(둘 다 아직 작업 시작 전 단계로 취급).
  - 룸체크 목록의 "전체 상태" 드롭다운에 "업체 의뢰함" 항목을 추가해 보수관리 목록의 탭과
    같은 기준으로 필터링할 수 있게 했습니다.

## 2026-09-11 업체 서류·계좌 정보 등록 + FM 업체 정보 표시

homes-fm-admin 업체 정보 수정 화면에 사업자등록번호 외에 서류 첨부와 대금 지급 계좌
정보가 없어 추가했습니다(homes-fm-admin 쪽 코드는 이 저장소가 아닌 homes-fm-admin
저장소에 따로 반영되어야 합니다. 이번 라운드에서는 homes-fm(FM) 쪽만 이 저장소에
반영했습니다).

- **[관리자 실행 필요] DB 마이그레이션 추가**: `maintenance_vendors` 테이블에
  `bank_name`(은행), `bank_account_no`(계좌번호), `bank_account_holder`(예금주명) 컬럼을
  추가하는 SQL을 `supabase_schema.sql` 맨 아래에 추가했습니다. 기존 `business_reg_no`
  컬럼 마이그레이션과 마찬가지로 Supabase 관리자가 대시보드 SQL 편집기에서 직접 실행해야
  합니다(AI가 대신 실행하지 않습니다).
- **homes-fm(FM) 업체 정보 표시**: 보수 항목의 "시공·수리 업체" 선택 영역 아래에
  사업자등록번호, 담당자 연락처, 입금계좌(은행/계좌번호/예금주명), 첨부된 사업자
  등록증·통장 사본 다운로드 버튼을 읽기 전용으로 보여주는 정보 박스를 추가했습니다
  (`vendorInfoHtml`). 업체 선택을 바꾸면 정보 박스도 함께 갱신됩니다. 등록·수정 화면 자체는
  homes-fm-admin에서만 제공되며, FM에서는 조회만 가능합니다.
- 기존 데이터와 Supabase 테이블 구조(이미 있던 컬럼)는 전혀 변경하지 않았습니다.

## 2026-09-11 (긴급 수정) 업체 사업자번호·계좌정보가 저장 후에도 다시 열면 비어 보이는 문제

사용자 확인: homes-fm-admin에서 업체 정보(사업자등록번호, 은행/계좌번호/예금주명)를 입력하고
저장했는데, 업체 목록을 다시 열면 값이 비어 있는 것처럼 보이는 문제를 신고해 주셔서 원인을
찾아 고쳤습니다.

- **원인**: DB에는 정상적으로 저장되고 있었지만, homes-fm이 업체 목록을 불러올 때 쓰는
  조회 쿼리(select 컬럼 목록)에 `business_reg_no`/`bank_name`/`bank_account_no`/
  `bank_account_holder` 컬럼이 빠져 있어서, 화면에 표시할 때마다 항상 빈 값으로만
  내려오고 있었습니다(저장 자체는 정상 — 조회 시 그 값만 안 가져온 것).
- **수정**: 업체 목록 조회 쿼리에 위 컬럼들을 모두 포함하도록 고쳤습니다. 아직 Supabase에
  `bank_name`/`bank_account_no`/`bank_account_holder` 컬럼을 추가하는 SQL을 실행하지
  않으셨다면, 그 컬럼들만 자동으로 빼고 조회하도록 안전장치도 함께 넣었습니다(기존
  `business_reg_no` 처리 방식과 동일).
- homes-fm-admin 쪽에도 동일한 원인의 버그가 있어 같은 방식으로 고쳤습니다(별도 zip).
- 기존 데이터와 Supabase 테이블 구조는 전혀 변경하지 않았습니다. **이미 입력해두신
  사업자등록번호·계좌정보가 있다면, 이번 수정이 반영된 앱을 다시 열어보시면 정상적으로
  보일 것입니다.** (단, `bank_name` 등 계좌 컬럼 자체가 아직 DB에 없다면 별도로 SQL을
  실행해 주셔야 계좌 정보가 저장·조회됩니다.)

## 2026-09-11 (수정) 보수 항목 화면에 사업자등록번호가 2번 표시되던 문제

사용자 확인: 업체를 선택하면 사업자등록번호가 위쪽 업체 정보 요약 박스와 아래쪽 "사업자
등록번호" 입력칸에 중복으로 나타나던 것을 지적해 주셨습니다.

- 아래쪽 "사업자 등록번호" 입력칸은 원래부터 있던, 저장·리포트 내보내기에 실제로 쓰이는
  필드입니다.
- 위쪽 업체 정보 요약 박스(vendorInfoHtml)에서는 사업자등록번호 줄을 빼고 담당자·연락처·
  입금계좌·첨부서류만 보여주도록 정리했습니다.

## 2026-09-11 "실제로 업체에 연락해서 의뢰함"(의뢰됨) 상태 구분 완전 제거

사용자 요청으로, 이전에 추가했던 "의뢰됨" 상태 단계와 관련 체크박스를 FM·ADMIN 양쪽에서
전부 제거했습니다.

- 보수 항목 화면의 "실제로 업체에 연락해서 의뢰함" 체크박스를 삭제했습니다.
- 보수관리 목록 상단 탭에서 "업체 의뢰함" 탭을 제거하고(전체/보수 예정/보수중/보수완료
  4개로 복귀), 룸체크 목록의 "전체 상태" 필터에서도 "업체 의뢰함" 옵션을 제거했습니다.
- 홈 화면 요약 타일, 룸체크 카드의 상태 배지·카운트칩, 리포트/엑셀 내보내기 라벨에서
  "의뢰됨" 관련 표시를 모두 제거하고 원래 단계(보수 예정/보수중/완료)로 되돌렸습니다.
- 이미 저장된 기존 기록에 `vendorRequested` 값이 남아 있어도 더 이상 어떤 화면에서도
  참조하지 않으므로 데이터 정리는 별도로 필요 없습니다.
- 기존 데이터와 Supabase 테이블 구조는 전혀 변경하지 않았습니다.

## 2026-09-11 "스튜디오 FM 매뉴얼 Ver.1.1" 참고 기능 3종 추가

사용자가 업로드한 "스튜디오 FM 매뉴얼(Ver.1.1)" 문서 중, 회사 공식 규정으로 아직
확정되지 않은 비용부담 매트릭스(§6·§8)를 제외하고 사용자가 선택한 3가지만 반영했습니다.

- **보수 완료 기한 SLA 기준 자동 제안**: 보수 상세 화면의 "보수 완료 기한" 아래에
  "SLA 기준 제안" 버튼을 추가했습니다. 매뉴얼 §13(긴급=즉시, 일반=영업일 5일 이내) 기준으로
  날짜를 채워주며, 이미 입력된 값이 있으면 덮어쓰기 전에 확인을 거칩니다. 확정된 사내
  SLA 규정이 아닌 참고 제안이라는 점을 화면에 안내 문구로 표시했습니다.
- **계절별 특별점검 안내(더보기 메뉴)**: 매뉴얼 §12(장마철·혹서기·혹한기 대비, 법정 점검
  주기)를 그대로 옮긴 읽기 전용 안내 화면을 "더보기" 메뉴에 추가했습니다.
- **"정기점검(법정·계절)" 공용부 체크리스트**: 매뉴얼 §12·§15 내용을 실제 체크 항목으로
  구성해 새 공용부 구역으로 추가했습니다(모든 지점, 모든 층에서 선택 가능). 관리자 계정으로
  접속하면 지점마다 아직 없는 경우에만 자동으로 DB에 등록되며(기존 다른 체크리스트 양식은
  전혀 건드리지 않음), homes-fm-admin의 "체크리스트 양식 관리"에서 그대로 열어 지점별로
  수정할 수 있습니다.
- **전용부 비용부담 참고표(초안, 더보기 메뉴)**: 매뉴얼 §6·§8을 옮긴 읽기 전용 참고
  화면을 추가했습니다. **문서 자체가 "확정 전 초안"이라고 명시**하고 있어, 앱에서 자동으로
  비용부담을 결정하는 데는 전혀 쓰이지 않으며 실제 costBearer 입력은 이전과 동일하게
  사용자가 직접 선택합니다. 화면 상단에 "확정 전 초안" 경고 문구를 넣었습니다.
- 기존 데이터와 Supabase 테이블 구조는 전혀 변경하지 않았습니다(새 체크리스트 템플릿
  행만 추가될 수 있음).

## 2026-09-11 지점 정리(병합) 관련 homes-fm 점검 결과 + 방어적 수정

homes-fm-admin에 추가한 "다른 지점에 합치기"로 중복 지점(아코모가산바이홈즈-101동/
-201동, 아코모회기바이홈즈)을 정리하면 homes-fm에도 자동으로 반영되는지 점검했습니다.

- **자동 반영됨**: homes-fm은 지점·동·층·호실 목록을 매번 Supabase `branches`/
  `branch_units` 테이블에서 직접 읽어오므로, ADMIN에서 지점을 합치고 "DB 저장"하면
  homes-fm은 코드 수정 없이 정리된 지점 목록을 그대로 보여줍니다.
- **점검 중 발견해 미리 손본 부분**: "아코모가산 지점 여부"를 판단하는 `isGasanBranch`
  (동 선택 단계를 보여줄지 결정하는 핵심 함수)가 그동안 지점명에 "아코모가산"이라는
  글자만 들어있으면 전부 인정하는 느슨한 방식(`.includes()`)이었습니다. 지금까지는 이
  느슨함 덕분에 "아코모가산바이홈즈-101동" 같은 이름에서도 동케이스가 어느정도 동작했지만,
  반대로 나중에 비슷한 이름의 지점이 실수로 또 생기면 잘못 인식될 위험이 있어
  **정확히 "아코모가산"/"아코모회기"와 일치할 때만** 인정하도록 정리했습니다
  (`isHoegiBranch`도 동일).
- 위 정리로 인해 지점을 "다른 지점에 합치기"로 정리한 뒤에는 아코모가산이 예전과 동일하게
  지점 선택 → 동 선택 → 층 선택 → 호실 선택 순서로 정상 동작합니다.
- 기존 데이터와 Supabase 테이블 구조는 전혀 변경하지 않았습니다.
