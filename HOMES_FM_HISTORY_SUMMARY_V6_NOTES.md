# HOMES FM 실무자용 v6 수정 notes

## 수정 항목
- 수리 이력 화면의 비용 부담 주체 요약 영역을 한 줄 배치로 변경
- 기존에는 2열 그리드라 `투자사(임대인)`, `당사(홈즈)` 다음 줄에 `기타`가 내려갔음
- 데스크톱/태블릿 폭에서는 3개 요약 카드가 한 줄로 표시되도록 CSS 오버라이드 추가
- 모바일 좁은 화면에서는 화면 폭에 맞춰 자동 줄바꿈 유지

## 데이터 영향
- Supabase 데이터 구조 변경 없음
- 저장/조회 로직 변경 없음
- 화면 표시 CSS만 수정
